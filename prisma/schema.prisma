generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Order {
  id         String   @id @default(cuid())
  name       String
  phone      String
  address    String
  quantity   Int
  totalPrice Int
  note       String?
  createdAt  DateTime @default(now())
  
}